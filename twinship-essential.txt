        "nebula-rose": "#ff1493",
        "stellar-blue": "#00bfff",
        "orbit-sage": "#00ff7f",
        "solar-amber": "#ffff00",
        "celestial-indigo": "#8a2be2",
        "comet-coral": "#ff4500",
        "aurora-teal": "#00ffff",
        "meteor-copper": "#ff0000",
  const themeColor = userProfile?.accentColor || "celestial-indigo";
      case "nebula-rose":
      case "stellar-blue":
      case "orbit-sage":
      case "solar-amber":
      case "celestial-indigo":
      case "comet-coral":
      case "aurora-teal":
      case "meteor-copper":
    case "nebula-rose":
    case "stellar-blue":
    case "orbit-sage":
    case "solar-amber":
    case "celestial-indigo":
    case "comet-coral":
    case "aurora-teal":
    case "meteor-copper":
    case "solar-amber":
    case "aurora-teal":
    case "orbit-sage":
    case "nebula-rose":
    case "stellar-blue":
    case "orbit-sage":
    case "solar-amber":
    case "celestial-indigo":
    case "comet-coral":
    case "aurora-teal":
    case "meteor-copper":
export type ThemeColor = "nebula-rose" | "stellar-blue" | "orbit-sage" | "solar-amber" | "celestial-indigo" | "comet-coral" | "aurora-teal" | "meteor-copper";
      themeColor: "celestial-indigo",
          themeColor: profile.accentColor || "celestial-indigo"
        themeColor: "celestial-indigo",
  const accentColor = userProfile?.accentColor || "celestial-indigo";
        accentColor: "celestial-indigo" as ThemeColor,
      accentColor: "celestial-indigo" as ThemeColor,
    const accent: ThemeColor = (meName === "Alex" ? "nebula-rose" : "celestial-indigo") as ThemeColor;
      accentColor: (userProfile?.accentColor === "celestial-indigo" ? "nebula-rose" : "celestial-indigo") as ThemeColor,
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || "celestial-indigo";
      case "nebula-rose": return "#ff1493";
      case "stellar-blue": return "#00bfff";
      case "orbit-sage": return "#00ff7f";
      case "solar-amber": return "#ffff00";
      case "celestial-indigo": return "#8a2be2";
      case "comet-coral": return "#ff4500";
      case "aurora-teal": return "#00ffff";
      case "meteor-copper": return "#ff0000";
  const themeColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || "celestial-indigo";
      case "nebula-rose": return "#ff1493";
      case "stellar-blue": return "#00bfff";
      case "orbit-sage": return "#00ff7f";
      case "solar-amber": return "#ffff00";
      case "celestial-indigo": return "#8a2be2";
      case "comet-coral": return "#ff4500";
      case "aurora-teal": return "#00ffff";
      case "meteor-copper": return "#ff0000";
  const themeColor = userProfile?.accentColor || "celestial-indigo";
      case "nebula-rose": return "#ff1493";
      case "stellar-blue": return "#00bfff";
      case "orbit-sage": return "#00ff7f";
      case "solar-amber": return "#ffff00";
      case "celestial-indigo": return "#8a2be2";
      case "comet-coral": return "#ff4500";
      case "aurora-teal": return "#00ffff";
      case "meteor-copper": return "#ff0000";
  const themeColor = userProfile?.accentColor || 'celestial-indigo';
    const themeColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = themeColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const themeColor = userProfile?.accentColor || "celestial-indigo";
  const themeColor = userProfile?.accentColor || 'celestial-indigo';
    userProfile?.accentColor || "celestial-indigo"
    { color: "nebula-rose", name: "Hot Pink", description: "Intuitive & Emotional" },
    { color: "stellar-blue", name: "Electric Blue", description: "Calm & Analytical" },
    { color: "orbit-sage", name: "Bright Green", description: "Balanced & Growth" },
    { color: "solar-amber", name: "Neon Yellow", description: "Energetic & Creative" },
    { color: "celestial-indigo", name: "Vibrant Purple", description: "Creative & Wise" },
    { color: "comet-coral", name: "Bright Orange", description: "Bold & Passionate" },
    { color: "aurora-teal", name: "Electric Cyan", description: "Clear & Focused" },
    { color: "meteor-copper", name: "Bright Red", description: "Strong & Determined" },
        accentColor: "celestial-indigo",
      accentColor: userProfile?.accentColor || "celestial-indigo",
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = twinProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || 'celestial-indigo';
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
  const accentColor = userProfile?.accentColor || "celestial-indigo";
